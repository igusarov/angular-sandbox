import { DcPointDirective } from './dc-point.directive';

describe('DcPointDirective', () => {
  it('should create an instance', () => {
    const directive = new DcPointDirective();
    expect(directive).toBeTruthy();
  });
});
